<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="PixelPower: Tienda de videojuegos.">

    <title><%= title %></title>

    <link rel="shortcut icon" href="/img/LogoPixelPower.png">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <link rel="stylesheet" href="/css/styles.css">

    <link href="https://fonts.googleapis.com/css2?family=Audiowide&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
          integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
          crossorigin="anonymous" referrerpolicy="no-referrer" />

</head>
<body class="bg-secondary">

<header class="header container-fluid bg-dark text-white py-2">
    <div class="container">
        <!-- Contenedor principal: en móvil se organiza en columna, en desktop en fila, con espacio entre elementos y alineación centrada -->
        <div class="d-flex flex-column flex-sm-row justify-content-between align-items-center position-relative">

            <!-- Logo: aparece arriba en móvil, a la izquierda en desktop -->
            <div class="d-flex align-items-center mb-2 mb-sm-0">
                <a href="/Administrador/CerrarSesion">
                    <img src="/img/LogoPixelPower.png" width="60" class="logo">
                </a>
            </div>

            <!-- Nombre de la página: centrado en móvil y desktop, pero en desktop está posicionado absolutamente en el centro -->
            <!-- translate-middle-sm: aplica transform: translate(-50%, -50%) solo en pantallas ≥ sm para centrar el elemento exactamente -->
            <div class="text-center mb-2 mb-sm-0 position-sm-absolute top-sm-50 start-sm-50 translate-middle-sm">
                <h1 class="m-0 fs-md-0"><%= NombreDeLaPagina %></h1>
            </div>

            <!-- Admin (abajo en móvil, derecha en desktop) -->
            <% if (admin) { %>
                <div class="d-flex flex-column flex-sm-row align-items-center mb-2 mb-sm-0">
                    <span class="fs-4 me-sm-2"><%= admin.nombre_de_usuario %></span>
                    <img src="<%= admin.foto %>" alt="Foto de perfil" width="60" height="60"
                         style="object-fit: cover; border-radius: 50%;" class="mt-1 mt-sm-0">
                </div>
            <% } %>
        </div>

        <!-- Botón Cerrar Sesión (abajo en móvil, derecha en desktop) -->
        <div class="text-center text-sm-end mt-2">
            <a href="/Administrador/CerrarSesion" class="btn btn-danger btn-sm">Cerrar sesión</a>
        </div>
    </div>
</header>


<main>
